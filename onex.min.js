class OnexPlayer {constructor() {this.audio = null;}init(src, options = {}) {if (!src) {console.error("[Onex] Debes especificar un archivo de audio");return;}this.audio = new Audio(src);this.audio.loop = options.loop ?? true;this.audio.volume = options.volume ?? 1;this.audio.play().catch(() => {console.warn("[Onex] El navegador bloqueó la reproducción automática. Usa Onex.play() luego de una interacción.");});}play() {this.audio?.play();}pause() {this.audio?.pause();}stop() {if (!this.audio) return;this.audio.pause();this.audio.currentTime = 0;}setVolume(vol) {if (!this.audio) return;this.audio.volume = vol;}toggle() {if (!this.audio) return;this.audio.paused ? this.audio.play() : this.audio.pause();}}window.Onex = new OnexPlayer();